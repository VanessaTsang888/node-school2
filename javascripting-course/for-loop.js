// Define a variable named: total and assign it to the number 0.
let total = 0;
// Define a variable named: limit and assign it to the number 10.
let limit = 10;

for (let i = 0; i < 10; i++) {
    // log the numbers 0 through 9.
    /* On each iteration of the loop, 
    add the number: i to the: total variable. */
    total += i;
    /* after the for loop, use: console.log()
    to print the: total variable to the terminal. */
}
console.log(total);