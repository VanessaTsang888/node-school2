/* Declare a variable called: fruit and reference that to a string value
 of "orange". */
const fruit = 'orange';

/* if the fruit name orange has more than five characters, then print the 
string below. */
if (fruit.length > 5) {
    console.log('The fruit name has more than five characters.');

    // Otherwise, print the below string to the console.    
} else {
    console.log('The fruit name has five character or less.');
}

/* I've used the command: node if-statement.js
and found it actually prints out the else-statement which is correct - yey! */